# URLs — GitHub Pages (repositório `catalogo`)

Como o GitHub Pages está publicando o repositório de projeto `catalogo`, a base pública é:

`https://deliverymacrofood-wq.github.io/catalogo/`

URLs corretas:
- Catálogo: `/catalogo/`
- Login: `/catalogo/login/`
- Administração: `/catalogo/admin/`

O erro `/catalogo/login/admin/` foi corrigido. Dentro de `login/`, o painel agora usa `../admin/`, que sobe para a pasta `catalogo` antes de entrar em `admin`.

Também foram corrigidos, dentro do login, os links para pedidos, suporte e retorno ao catálogo.
